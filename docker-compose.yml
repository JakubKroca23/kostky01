version: '3.8'
services:
  kostky-game:
    build: .
    container_name: kostky-game
    restart: always
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      # - CLIENT_URL=https://kostky.tvadomena.cz # If you need explicit CORS
